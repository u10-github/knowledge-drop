1. 対象リポジトリで Issues を有効にする
2. `.github/ISSUE_TEMPLATE/` ディレクトリを作成する
3. `inbox-share.yml` と `inbox-manual.yml` を配置する
4. `config.yml` を配置して blank issue を抑制する
5. 変更を commit / push したあと、GitHub の New issue 画面でフォームが選べることを確認する
